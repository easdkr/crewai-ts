# Publishes all @crewai-ts/* packages to npm when a version tag is pushed.
# Auth uses npm OIDC Trusted Publishing — no NPM_TOKEN secret is required.
# One-time setup: configure this repo + workflow as a Trusted Publisher on
# npmjs.com for each package (package → Settings → Trusted Publisher).

name: Publish to npm

on:
  push:
    tags:
      - "v*"

permissions:
  contents: read
  id-token: write # required for npm OIDC trusted publishing + provenance

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9.15.0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "pnpm"

      # OIDC trusted publishing requires npm >= 11.5.1; Node 22 bundles npm 10.x.
      - name: Upgrade npm
        run: npm install -g npm@latest

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # Build first: type-aware lint relies on built output in some packages.
      - name: Build
        run: pnpm -r build

      - name: Lint
        run: pnpm -r lint

      - name: Test
        run: pnpm -r test

      - name: Verify tag matches @crewai-ts/core version
        run: |
          PKG_VERSION="v$(node -p "require('./packages/core/package.json').version")"
          if [ "$PKG_VERSION" != "$GITHUB_REF_NAME" ]; then
            echo "::error::Tag $GITHUB_REF_NAME does not match @crewai-ts/core version $PKG_VERSION"
            exit 1
          fi

      # Publish all workspace packages in dependency order.
      # Core first, then packages that depend only on core,
      # then packages with cross-dependencies.
      - name: Publish @crewai-ts/core
        run: |
          VERSION="$(node -p "require('./packages/core/package.json').version")"
          if npm view "@crewai-ts/core@${VERSION}" version >/dev/null 2>&1; then
            echo "::notice::@crewai-ts/core@${VERSION} already published, skipping"
          else
            pnpm -F @crewai-ts/core exec npm publish --access public --provenance
          fi

      - name: Publish @crewai-ts/openai
        run: |
          VERSION="$(node -p "require('./packages/openai/package.json').version")"
          if npm view "@crewai-ts/openai@${VERSION}" version >/dev/null 2>&1; then
            echo "::notice::@crewai-ts/openai@${VERSION} already published, skipping"
          else
            pnpm -F @crewai-ts/openai exec npm publish --access public --provenance
          fi

      - name: Publish @crewai-ts/anthropic
        run: |
          VERSION="$(node -p "require('./packages/anthropic/package.json').version")"
          if npm view "@crewai-ts/anthropic@${VERSION}" version >/dev/null 2>&1; then
            echo "::notice::@crewai-ts/anthropic@${VERSION} already published, skipping"
          else
            pnpm -F @crewai-ts/anthropic exec npm publish --access public --provenance
          fi

      - name: Publish @crewai-ts/gemini
        run: |
          VERSION="$(node -p "require('./packages/gemini/package.json').version")"
          if npm view "@crewai-ts/gemini@${VERSION}" version >/dev/null 2>&1; then
            echo "::notice::@crewai-ts/gemini@${VERSION} already published, skipping"
          else
            pnpm -F @crewai-ts/gemini exec npm publish --access public --provenance
          fi

      - name: Publish @crewai-ts/bedrock
        run: |
          VERSION="$(node -p "require('./packages/bedrock/package.json').version")"
          if npm view "@crewai-ts/bedrock@${VERSION}" version >/dev/null 2>&1; then
            echo "::notice::@crewai-ts/bedrock@${VERSION} already published, skipping"
          else
            pnpm -F @crewai-ts/bedrock exec npm publish --access public --provenance
          fi

      - name: Publish @crewai-ts/azure
        run: |
          VERSION="$(node -p "require('./packages/azure/package.json').version")"
          if npm view "@crewai-ts/azure@${VERSION}" version >/dev/null 2>&1; then
            echo "::notice::@crewai-ts/azure@${VERSION} already published, skipping"
          else
            pnpm -F @crewai-ts/azure exec npm publish --access public --provenance
          fi

      - name: Publish @crewai-ts/snowflake
        run: |
          VERSION="$(node -p "require('./packages/snowflake/package.json').version")"
          if npm view "@crewai-ts/snowflake@${VERSION}" version >/dev/null 2>&1; then
            echo "::notice::@crewai-ts/snowflake@${VERSION} already published, skipping"
          else
            pnpm -F @crewai-ts/snowflake exec npm publish --access public --provenance
          fi

      - name: Publish @crewai-ts/rag
        run: |
          VERSION="$(node -p "require('./packages/rag/package.json').version")"
          if npm view "@crewai-ts/rag@${VERSION}" version >/dev/null 2>&1; then
            echo "::notice::@crewai-ts/rag@${VERSION} already published, skipping"
          else
            pnpm -F @crewai-ts/rag exec npm publish --access public --provenance
          fi

      - name: Publish @crewai-ts/mcp
        run: |
          VERSION="$(node -p "require('./packages/mcp/package.json').version")"
          if npm view "@crewai-ts/mcp@${VERSION}" version >/dev/null 2>&1; then
            echo "::notice::@crewai-ts/mcp@${VERSION} already published, skipping"
          else
            pnpm -F @crewai-ts/mcp exec npm publish --access public --provenance
          fi

      - name: Publish @crewai-ts/a2a
        run: |
          VERSION="$(node -p "require('./packages/a2a/package.json').version")"
          if npm view "@crewai-ts/a2a@${VERSION}" version >/dev/null 2>&1; then
            echo "::notice::@crewai-ts/a2a@${VERSION} already published, skipping"
          else
            pnpm -F @crewai-ts/a2a exec npm publish --access public --provenance
          fi

      - name: Publish @crewai-ts/flow
        run: |
          VERSION="$(node -p "require('./packages/flow/package.json').version")"
          if npm view "@crewai-ts/flow@${VERSION}" version >/dev/null 2>&1; then
            echo "::notice::@crewai-ts/flow@${VERSION} already published, skipping"
          else
            pnpm -F @crewai-ts/flow exec npm publish --access public --provenance
          fi

      - name: Publish @crewai-ts/nestjs
        run: |
          VERSION="$(node -p "require('./packages/nestjs/package.json').version")"
          if npm view "@crewai-ts/nestjs@${VERSION}" version >/dev/null 2>&1; then
            echo "::notice::@crewai-ts/nestjs@${VERSION} already published, skipping"
          else
            pnpm -F @crewai-ts/nestjs exec npm publish --access public --provenance
          fi

      - name: Publish @crewai-ts/cli
        run: |
          VERSION="$(node -p "require('./packages/cli/package.json').version")"
          if npm view "@crewai-ts/cli@${VERSION}" version >/dev/null 2>&1; then
            echo "::notice::@crewai-ts/cli@${VERSION} already published, skipping"
          else
            pnpm -F @crewai-ts/cli exec npm publish --access public --provenance
          fi
