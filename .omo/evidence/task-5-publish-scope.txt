=== publish.yml package scope + follow-up comment ===

--- Package references in publish.yml ---
1:# Follow-up: publish workflows for @crewai-ts/nestjs and @crewai-ts/cli will be
2:# added in a separate PR. This workflow publishes @crewai-ts/core only.
5:# Publishes @crewai-ts/core to npm when a version tag (vX.Y.Z) is pushed.
54:      - name: Verify tag matches @crewai-ts/core version
58:            echo "::error::Tag $GITHUB_REF_NAME does not match @crewai-ts/core version $PKG_VERSION"
64:      # `pnpm -F @crewai-ts/core exec npm publish` runs npm publish scoped to
65:      # the @crewai-ts/core workspace package only.
66:      - name: Publish @crewai-ts/core
67:        run: pnpm -F @crewai-ts/core exec npm publish --access public --provenance

--- Follow-up comment present? ---
1:# Follow-up: publish workflows for @crewai-ts/nestjs and @crewai-ts/cli will be

--- Publish invocation ---
64:      # `pnpm -F @crewai-ts/core exec npm publish` runs npm publish scoped to
67:        run: pnpm -F @crewai-ts/core exec npm publish --access public --provenance

--- Verdict: PASS (only @crewai-ts/core is published; nestjs/cli are mentioned only in follow-up comment) ---
