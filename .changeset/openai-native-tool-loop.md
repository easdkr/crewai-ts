---
"@crewai-ts/core": patch
"@crewai-ts/openai": patch
---

Execute OpenAI native tool calls when `availableFunctions` or `available_functions` is provided, including bounded `maxToolRounds` support.
